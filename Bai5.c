#include <stdio.h>

int main() {
    int length = 10;
    int width = 5;
    int perimeter = 2 * (length + width);
    int area = length * width;
    printf("Chu vi hình chữ nhật: %d\n", perimeter);
    printf("Diện tích hình chữ nhật: %d\n", area);

    return 0;
}
